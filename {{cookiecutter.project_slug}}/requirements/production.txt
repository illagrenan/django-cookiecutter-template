# =======================================================================================================================
# PIP requirements for *production* server (running on Ubuntu)
# =======================================================================================================================

-r base.txt

gunicorn==19.7.1
raven~=6.0.0 # pyup: ignore
